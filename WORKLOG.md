# Work Log

## 2025-07-19

### 완료

- [x] 메세지 큐 구조 변경
  - GetMessage()에서 PeekMessage()로 변경
- [x] Core 클래스 생성
  - 싱글톤 패턴
- [x] CKeyMgr 클래스 생성
- [x] define.h, pch.h(미리 컴파일된 헤더) 추가
- [x] 싱글톤 매크로 추가
- [x] CCore 클래스 init 추가

### 중요 개념

- 매크로
- 싱글톤

## 2025-07-20

### 완료

- [x] CObject 클래스 생성

## 2025-07-23

### 완료

- [x] CTimeMgr 클래스 생성 및 구현
- [x] double buffering 테스트
- [x] 키 상태, 키값 매핑을 위한 enum.h 생성
- [x] CKeyMgr 클래스 구현
  - 목적 : 윈도우에서 제공하는 함수는 현재 상태만 알 수 있어서 이벤트 감지가 어려움

### 중요 개념

- 시간 동기화
- 더블 버퍼링

<details>
  <summary>개발 회고</summary>

## 전체 구조 개선

### 메시지 루프 변경

- **문제점**: `GetMessage()` 함수는 메시지 큐가 비어있는 시간이 많아 비효율적
- **해결책**: `PeekMessage()` 함수로 변경하여 메시지가 없는 시간을 활용
- **결과**: 메시지가 없을 때 `CCore::GetInst()->progress()` 함수를 지속적으로 호출

## CCore (핵심 엔진)

### 초기화 과정

1. 메시지 루프 전에 CCore 싱글톤 객체 생성
2. 메인 윈도우 정보 전달
   - 윈도우 핸들 (hWnd)
   - 윈도우 크기 (ptResolution)
3. 윈도우 크기를 버전에 맞게 조정
4. 더블 버퍼링을 위한 비트맵 생성
5. 각 매니저 싱글톤 객체 생성 및 초기화
6. 임시 글로벌 오브젝트 생성

### Progress 함수

- 각 매니저 업데이트
- 임시 오브젝트(g_obj) 업데이트
- 렌더링 처리

### 현재 오브젝트 구조

- 단순히 위치(Vec2)와 크기(Vec2)만 보유

## CTimeMgr (시간 관리자)

### 문제점

- 컴퓨터 성능에 따라 함수 호출 빈도가 달라짐
- 같은 시간에도 성능에 따라 처리량이 다름

### 해결 방법

1. **초기화**

   - 이전 카운트 값 (m_llPrevCount) 저장
   - 초당 카운트 횟수 (m_llFrequency) 저장

2. **업데이트**

   - 현재 카운트 값 (m_llCurCount) 측정
   - 한 프레임당 시간 (m_dDT) 계산
   - DT를 연산에 곱하여 프레임 독립적인 동작 구현

3. **추가 기능**
   - 윈도우 타이틀에 FPS 정보 표시

## CKeyMgr (키 입력 관리자)

### 문제점

- Windows의 `GetAsyncKeyState()`는 현재 상태만 제공
- 키 이벤트(방금 눌림, 방금 뗌) 감지 어려움

### 해결 방법

1. **키 상태 정의**

   - NONE: 이전에도 안눌림, 지금도 안눌림
   - TAP: 이전에 안눌림, 지금 눌림
   - HOLD: 이전에도 눌림, 지금도 눌림
   - AWAY: 이전에 눌림, 지금 안눌림

2. **구현 내용**

   - `m_vecKey` 벡터에 각 키 정보 저장
   - 키 상태와 이전 프레임 눌림 여부 추적
   - 윈도우 포커싱 상태 처리
   - 매 프레임마다 모든 키 상태 업데이트

3. **사용 방법**
   - CKeyMgr 싱글톤의 멤버 함수 호출로 키 상태 확인
   - `IsKeyTap()`, `IsKeyHold()`, `IsKeyAway()` 등 편의 함수 제공

</details>

---

### 완료

- [x] CScene 클래스 생성
- [x] CObject Update와 Render 추가
- [x] Player, Monster 오브젝트 추가
- [x] Vec2 연산자 오버로딩
- [x] Scene 전환 시 사용할 DeleteAllObject 함수
- [x] Collider 컴포넌트와 매니저 추가

### 중요 개념

- 순수 가상 함수 - 자식 클래스는 이 함수를 반드시 구현해야 한다.
- 자식 클래스의 소멸자를 호출해야 하기 때문에 소멸자에 'virtual' 키워드 필요
- 컴포넌트 구조 - 클래스 상속만으로는 유동성이 부족

<details>
  <summary>개발 회고</summary>

## 전체 구조 개선

### 게임 아키텍처 설계

- **문제점**: 단일 전역 오브젝트(g_obj)로는 복잡한 게임 구현 불가
- **해결책**: Scene 시스템과 Object 상속 구조 도입
- **결과**: 상황별로 다른 오브젝트들을 효율적으로 관리 가능

## CScene & CSceneMgr (씬 시스템)

### Scene 구조

1. **기본 Scene 클래스**

   - 오브젝트들을 그룹별로 관리 (m_arrObj)
   - Enter/Exit 가상 함수로 씬 전환 시 초기화/정리
   - Update/Render로 모든 오브젝트 일괄 처리

2. **Scene Manager**

   - 싱글톤 패턴으로 구현
   - 모든 씬을 배열로 관리
   - T키로 씬 전환 기능 구현

3. **구현된 씬**
   - CScene_Start: 플레이어 1개, 몬스터 1개
   - CScene_Tool: 플레이어 1개, 몬스터 5개 (테스트용)

### 메모리 관리

- **문제점**: 씬 전환 시 오브젝트 메모리 누수 발생
- **해결책**: Exit() 함수에서 DeleteAllObject() 호출
- **결과**: 씬 전환 시 안전한 메모리 정리

## CObject (오브젝트 시스템)

### 상속 구조

1. **기본 Object 클래스**

   - 위치(Vec2), 크기(Vec2), 생존 여부(bool) 보유
   - Update()는 순수 가상 함수로 자식이 구현
   - Render()는 기본 사각형 렌더링 제공
   - 가상 소멸자로 안전한 다형성 보장

2. **Player 클래스**

   - 키 입력 처리 (방향키 이동, 스페이스 리셋)
   - Update()에서 이동 로직 구현

3. **Monster 클래스**
   - 자동 좌우 왕복 이동
   - 중심점 기준 최대 거리까지 이동 후 방향 전환

## CCollider & CCollisionMgr (충돌 시스템)

### Collider 구조

1. **충돌체 컴포넌트**

   - 오브젝트와 별도로 충돌 영역 관리
   - 오프셋과 크기로 충돌 박스 정의
   - 고유 ID로 충돌 쌍 식별
   - 디버그용 초록색 박스 렌더링

2. **충돌 검사**
   - AABB(Axis-Aligned Bounding Box) 방식
   - 사각형 간 겹침 여부로 충돌 판정

### CollisionMgr 구조

1. **그룹별 충돌 관리**

   - GROUP_TYPE으로 오브젝트 분류 (PLAYER, MONSTER 등)
   - 비트 연산으로 충돌 체크할 그룹 쌍 설정
   - 그룹 간 모든 오브젝트 충돌 검사

2. **충돌 상태 추적**

   - map으로 충돌 쌍의 이전 상태 저장
   - Enter(충돌 시작), Stay(충돌 중), Exit(충돌 끝) 구분
   - 각 상태별로 다른 콜백 함수 호출

3. **충돌 콜백 시스템**
   - Object 클래스에 가상 함수로 정의
   - OnCollisionEnter/OnCollision/OnCollisionExit
   - 자식 클래스에서 필요시 재정의

## 유틸리티 개선

### Vec2 구조체 확장

- **추가된 연산자**: +, -, _, /, +=, -=, _=, /=, ==, !=
- **유틸리티 함수**: Length(), Normalize()
- **결과**: 벡터 연산 코드 가독성 대폭 향상

## 향후 계획

- **별의 커비 구현**을 위한 추가 기능
  - 적 흡수 메카닉
  - 능력 복사 시스템
  - 공중 부양
  - 투사체 시스템

</details>

---

## 2025-07-24

### 완료

- [x] 이벤트 매니저 생성
- [x] 이벤트 관련 매크로 추가

### 중요 개념

<details>
  <summary>이벤트 매니저 개발 회고</summary>

## 이벤트 매니저 도입 배경

### 기존 시스템의 문제점

- **동시성 문제**: 충돌 시 즉시 처리로 인한 예측 불가능한 실행 순서
- **Update 중 삭제 위험**: 오브젝트 순회 중 삭제 시 크래시 발생 가능
- **씬 전환 타이밍**: T키 입력 시 즉시 씬 전환으로 인한 불안정성
- **확장성 부족**: 새로운 이벤트 추가 시 각 클래스마다 개별 처리 필요

### 해결 목표

- 한 프레임 내 모든 이벤트의 동시성 보장
- 안전한 오브젝트 생명주기 관리
- 확장 가능한 이벤트 시스템 구축

## CEventMgr 아키텍처

### 핵심 설계 원칙

1. **지연 처리 (Deferred Execution)**

   - 이벤트 발생 시 즉시 실행 X → 큐에 저장
   - 프레임 마지막에 일괄 처리
   - 실행 순서 보장 및 안정성 확보

2. **이벤트 타입 시스템**

   ```cpp
   enum class EVENT_TYPE
   {
       CREATE_OBJECT,    // 오브젝트 생성
       DELETE_OBJECT,    // 오브젝트 삭제
       SCENE_CHANGE,     // 씬 변경
       COLLISION_ENTER,  // 충돌 시작
       COLLISION_EXIT,   // 충돌 종료
   }
   ```

3. **가비지 컬렉션**
   - 삭제 예정 오브젝트를 즉시 삭제하지 않고 다음 프레임에 처리
   - Update 도중 오브젝트 삭제로 인한 메모리 오류 방지

### 구현 세부사항

1. **이벤트 구조체**

   ```cpp
   struct tEvent
   {
       EVENT_TYPE eType;    // 이벤트 종류
       DWORD_PTR wParam;    // 첫 번째 매개변수
       DWORD_PTR lParam;    // 두 번째 매개변수
   }
   ```

2. **실행 순서**
   - ClearGarbageObject() → 이전 프레임 삭제 대상 정리
   - Execute() → 현재 프레임 이벤트 처리
   - m_vecEvent.clear() → 처리된 이벤트 클리어

## 기존 시스템 통합

### 1. 충돌 시스템 개선

**변경 전**: 충돌 즉시 콜백 호출

```cpp
pLeftCol->OnCollisionEnter(pRightCol);  // 즉시 실행
```

**변경 후**: 이벤트로 지연 처리

```cpp
tEvent event(EVENT_TYPE::COLLISION_ENTER, (DWORD_PTR)pLeftCol, (DWORD_PTR)pRightCol);
CEventMgr::GetInst()->AddEvent(event);  // 큐에 저장
```

**결과**: OnCollision은 매 프레임 호출되어야 하므로 즉시 처리 유지

### 편의성 향상

**매크로 정의**로 사용성 개선:

```cpp
#define CREATE_OBJECT(obj, group) // 오브젝트 생성 이벤트
#define DELETE_OBJECT(obj)        // 오브젝트 삭제 이벤트
#define CHANGE_SCENE(scene)       // 씬 변경 이벤트
```

## 개선 효과

### 안정성 향상

- **메모리 안전성**: Update 중 오브젝트 삭제 시 크래시 방지
- **예측 가능한 실행**: 모든 이벤트가 정해진 순서로 처리
- **상태 일관성**: 프레임 내 모든 객체가 동일한 상태 기준으로 처리

</details>

---

### 완료

- [x] 카메라 기능 생성

<details>
  <summary>카메라 시스템 개발 회고</summary>

## 카메라 시스템 도입 배경

### 기존 시스템의 한계점

- **고정된 시점**: 모든 오브젝트가 화면 해상도 내에서만 활동
- **제한된 월드 크기**: 1280x768 화면 크기가 곧 게임 월드 전체
- **시각적 단조로움**: 정적인 화면으로 인한 몰입도 저하
- **확장성 부족**: 큰 맵이나 다양한 연출 효과 구현 불가

### 해결 목표

- 큰 게임 월드와 작은 화면의 분리
- 동적이고 생동감 있는 카메라 연출
- 플레이어 추적 및 자동 카메라 기능
- 타격감을 위한 화면 효과 시스템

## CCamera 아키텍처 설계

### 핵심 설계 원칙

**좌표 시스템 분리**

```cpp
// 월드 좌표: 오브젝트가 실제 존재하는 위치 (게임 로직용)
Vec2 worldPos = player->GetPos();

// 화면 좌표: 실제로 그려지는 위치 (렌더링용)
Vec2 screenPos = CCamera::GetInst()->GetRenderPos(worldPos);
```

### 좌표 변환 시스템

1. **변환 공식**

   ```cpp
   m_vDiff = m_vCurLookAt - (해상도 / 2);
   화면좌표 = 월드좌표 - m_vDiff;
   월드좌표 = 화면좌표 + m_vDiff;
   ```

2. **CalDiff() 함수의 역할**
   - 화면 중심과 카메라 시점 간의 차이값 계산
   - 모든 오브젝트 렌더링 시 이 차이값을 적용

### 충돌 시스템 호환성

- **충돌 계산**: 월드 좌표에서 수행 (카메라 영향 없음)
- **충돌체 렌더링**: 화면 좌표로 변환하여 표시
- **일관성 유지**: 게임 로직과 시각적 표현의 분리

</details>

---

## 2025-07-25

### 완료

- [x] Path 매니저, Resource 매니저 추가
- 빌드 경로 변경

### 중요 개념

- 매니저 초기화 순서 주의

<details>
  <summary>리소스 매니저 개발 회고</summary>

## CPathMgr (경로 관리자)

### 문제점

- 리소스 파일들의 경로를 하드코딩하면 프로젝트 구조 변경 시 문제 발생
- 개발 환경과 배포 환경에서 상대 경로가 달라질 수 있음
- 각 클래스에서 개별적으로 경로를 처리하면 관리가 어려움

### 해결 방법

1. **경로 중앙화**

   - 모든 리소스 경로를 하나의 매니저에서 관리
   - Content 폴더를 기준으로 한 상대 경로 시스템 구축

2. **동적 경로 계산**

   - `GetCurrentDirectory()`로 실행 파일의 현재 작업 디렉토리 획득
   - `bin` 폴더 구조를 기반으로 Content 폴더까지의 절대 경로 계산
   - `m_strContentPath`에 "프로젝트루트\\bin\\content\\" 경로 저장

3. **상대 경로 지원**
   - `GetRelativePath()` 함수로 절대 경로에서 Content 경로 부분 제거
   - 리소스의 상대 경로만으로 파일 관리 가능

### 폴더 구조

```
KirbyClone/
├── bin/
│   ├── content/          ← 모든 리소스 파일
│   │   ├── texture/
│   │   ├── sound/
│   │   └── data/
│   └── x64/
│       ├── Debug/        ← 실행 파일 위치
│       └── Release/
```

## CResMgr (리소스 관리자)

### 문제점

- 같은 리소스를 여러 번 로드하면 메모리 낭비
- 리소스 파일 경로 관리의 복잡성
- 리소스 해제 시점 관리 어려움

### 해결 방법

1. **리소스 캐싱 시스템**

   - `map<wstring, CRes*> m_mapTex`로 로드된 텍스처 관리
   - 키(문자열)를 통한 리소스 식별 및 재사용
   - `FindTexture()` 함수로 이미 로드된 리소스 확인

2. **자동 경로 처리**

   - CPathMgr과 연동하여 상대 경로를 절대 경로로 변환
   - `LoadTexture(키, 상대경로)` 형태로 간편한 인터페이스 제공

3. **메모리 관리**
   - 소멸자에서 모든 리소스 자동 해제
   - 중복 로드 방지로 메모리 효율성 향상

### 사용 예시

```cpp
// 텍스처 로드 (한 번만 로드됨)
CTexture* pTex = CResMgr::GetInst()->LoadTexture(L"Player", L"texture\\Player.bmp");

// 같은 키로 다시 호출 시 기존 텍스처 반환 (재로드 없음)
CTexture* pSameTex = CResMgr::GetInst()->LoadTexture(L"Player", L"texture\\Player.bmp");
```

## CRes & CTexture (리소스 기반 클래스)

### 설계 구조

1. **CRes (추상 기반 클래스)**

   - 모든 리소스의 공통 속성 정의
   - 키값(m_strKey)과 상대경로(m_strRelativePath) 관리
   - 확장성을 위한 가상 소멸자 제공

2. **CTexture (구체 클래스)**
   - CRes를 상속받아 비트맵 리소스 전용 기능 구현
   - HDC, HBITMAP을 통한 Windows GDI 연동
   - `Load()` 함수에서 HRESULT 반환으로 오류 처리

### 주요 개선 사항

- **초기화 순서 해결**: CPathMgr, CResMgr를 CSceneMgr보다 먼저 초기화
- **오류 처리**: 파일 로드 실패 시 적절한 메시지와 함께 E_FAIL 반환
- **호환성**: CObject와 연동하여 기존 사각형 렌더링과 텍스처 렌더링 선택 가능

### 확장성

- 향후 사운드(CSound), 애니메이션(CAnimation) 등 다른 리소스 타입 추가 용이
- 리소스 타입별 전용 맵 구조로 확장 가능한 설계
</details>

### 완료

- [x] FSM과 이에 따른 애니메이션 추가

### 중요 개념

- FSM

<details>
  <summary>애니메이션 & FSM 개발 회고</summary>

## 애니메이션 시스템 구현

### CAnimation (개별 애니메이션 관리)

- **목적**: 스프라이트 시트에서 프레임별 애니메이션 데이터 관리
- **구조**:
  - `tAnimFrame`: 각 프레임의 좌표, 크기, 지속시간 정보
  - `m_vecFrame`: 애니메이션을 구성하는 프레임들의 벡터
  - `m_iCurFrame`: 현재 재생 중인 프레임 인덱스
  - `m_fAccTime`: 프레임 전환을 위한 누적 시간

### 핵심 기능

1. **Create()**: 스프라이트 시트에서 연속된 프레임들을 자동 생성
   - 시작 좌표, 프레임 크기, 간격, 지속시간, 프레임 수 설정
2. **Update()**: DeltaTime 기반 프레임 전환 처리
3. **Render()**: BitBlt를 사용한 프레임별 렌더링
4. **Reset()**: 애니메이션 처음부터 재시작

## CAnimator (애니메이션 관리자)

### 설계 목적

- **문제점**: 오브젝트마다 여러 애니메이션을 개별 관리하기 복잡
- **해결책**: 하나의 컴포넌트로 모든 애니메이션을 통합 관리

### 구현 내용

1. **애니메이션 저장소**

   - `map<wstring, CAnimation*>`: 이름으로 애니메이션 관리
   - 동적 생성/삭제 및 안전한 메모리 관리

2. **재생 제어**

   - `Play()`: 지정된 애니메이션으로 전환
   - 반복 재생 여부 설정 가능
   - 자동 리셋으로 부드러운 전환

3. **Owner 연결**
   - `friend class CObject`로 안전한 소유권 관리
   - 렌더링 시 소유자 오브젝트 위치 자동 참조

## FSM (Finite State Machine) 구현

### 상태 정의

```cpp
enum class PLAYER_STATE
{
    IDLE,   // 대기 상태
    WALK,   // 걷기 상태
    JUMP,   // 점프 상태
    END
};
```

### 상태 전환 로직

1. **입력 기반 상태 결정**

   - 바닥 여부 우선 확인 (점프 상태)
   - 이동 입력 확인 (걷기 상태)
   - 기본값은 대기 상태

2. **상태 변경 시 처리**
   - `ChangeState()`: 이전 상태 저장 후 새 상태로 전환
   - 상태별 애니메이션 자동 재생
   - 반복/비반복 설정 (점프는 1회, 나머지는 반복)

## 물리 시스템과의 연동

### 중력 및 점프 구현

- **중력**: 매 프레임 `m_fGravity * DT`만큼 Y 속도 증가
- **점프**: 스페이스바 + 바닥 접촉 시에만 점프력 적용
- **바닥 충돌**: 간단한 Y좌표 기반 바닥 감지

### 상태와 물리의 동기화

- 물리 상태(바닥 접촉, 이동 속도)가 FSM 상태 결정
- FSM 상태가 애니메이션 선택
- 일관된 캐릭터 동작 보장

## 메모리 관리 개선

### 소멸자 안전성 확보

- **문제점**: 씬 전환 시 iterator 접근 위반 에러
- **해결책**:
  ```cpp
  // 안전한 map 순회 및 삭제
  for (auto iter = m_mapAnim.begin(); iter != m_mapAnim.end(); )
  {
      delete iter->second;
      iter = m_mapAnim.erase(iter);
  }
  ```

### Component 패턴 적용

- CObject가 CAnimator를 소유하는 구조
- `CreateAnimator()`: 부모 클래스에서 안전한 생성
- `GetAnimator()`: 접근자 함수로 캡슐화 유지

## 확장 가능한 구조

### 애니메이션 확장성

- 스프라이트 시트 좌표만 수정하면 새 애니메이션 추가 가능
- 프레임별 지속시간 개별 설정 가능
- 반복/비반복 애니메이션 지원

### FSM 확장성

- 새로운 상태 추가 시 enum과 switch문만 수정
- 상태별 진입/종료 로직 추가 가능
- 복잡한 상태 조건 설정 가능

</details>

---

### 완료

- [x] 강체(RigidBody) 컴포넌트 클래스 추가

<details>
  <summary>RigidBody 컴포넌트 개발 회고</summary>

## RigidBody 시스템 구현

### 설계 목적

- **문제점**: Player 클래스에 물리 로직이 하드코딩되어 복잡성 증가
- **해결책**: 물리 연산을 전담하는 독립적인 컴포넌트 설계
- **장점**: 재사용성, 확장성, 유지보수성 향상

## 물리 시스템 핵심 구조

### 물리량 관리

```cpp
Vec2 m_vVelocity;    // 속도 벡터
Vec2 m_vAccel;       // 가속도 벡터
Vec2 m_vForce;       // 힘 벡터 (매 프레임 초기화)
float m_fMass;       // 질량
```

### 물리 법칙 구현

1. **뉴턴 제2법칙**: `F = ma` → `a = F/m`
2. **운동 방정식**: `v = v₀ + at`, `s = s₀ + vt`
3. **중력**: 전역 중력값 × 개별 중력 스케일
4. **마찰**: 바닥 접촉 시에만 속도 감소

## 중력 시스템 설계

### 전역 중력 관리

- `static float s_fGravity`: 모든 객체 공통 중력값
- `float m_fGravityScale`: 객체별 중력 배율 (기본 1.0)
- **실제 중력**: `s_fGravity × m_fGravityScale`

### 중력 적용 조건

```cpp
if (m_bUseGravity && !m_bGround)
{
    m_vAccel.y += s_fGravity * m_fGravityScale;
}
```

## 마찰 시스템 구현

### 물리적 마찰 모델

- **마찰력**: `f = μ × N` (μ: 마찰계수, N: 수직항력)
- **적용 조건**: 바닥 접촉 시에만
- **방향성**: 운동 방향 반대로 작용

### 구현 방법

```cpp
if (m_bGround && abs(m_vVelocity.x) > 0.1f)
{
    float fFrictionForce = m_fFriction * s_fGravity * fDT;
    // 속도 방향 반대로 마찰력 적용
}
```

## 컴포넌트 패턴 적용

### CObject 연동 구조

- **생성**: `CreateRigidBody()` 함수로 안전한 생성
- **소유권**: CObject가 소유, friend 관계로 접근 제어
- **생명주기**: CObject 소멸 시 자동 정리

### 인터페이스 설계

1. **직접 제어**: `SetVelocityX/Y()` - 즉시 속도 변경
2. **물리적 힘**: `AddForce()` - 질량 기반 가속도 적용
3. **충격량**: `AddImpulse()` - 질량 무관 즉시 속도 변경

## 점프 시스템 구현 과정

### 초기 문제점

```cpp
// 문제가 된 코드
if (vPos.y >= 400.f)  // 위치만 확인
{
    SetVelocityY(0.f);  // 점프 속도 즉시 제거
}
```

### 문제 분석

1. **점프 직후**: Y속도 = -400, Y위치 = 400 (바닥)
2. **다음 프레임**: 위치가 여전히 400이므로 조건문 진입
3. **속도 리셋**: `SetVelocityY(0.f)`로 점프 무효화

### 해결 방법

```cpp
// 속도 방향 고려한 바닥 충돌
if (vPos.y >= 400.f && vVelocity.y >= 0.f)
{
    // 떨어지고 있을 때만 바닥 충돌 처리
}
```

## 물리 시뮬레이션 순서

### Update() 함수 실행 순서

1. **힘 → 가속도**: F = ma 공식 적용
2. **중력 추가**: 바닥이 아닐 때만
3. **가속도 → 속도**: v = v₀ + at
4. **마찰 적용**: 바닥에서만 속도 감소
5. **속도 제한**: 최대 속도 클램핑
6. **속도 → 위치**: s = s₀ + vt
7. **힘 초기화**: 다음 프레임을 위해 리셋

## 확장 가능한 구조

### 매개변수 설정

- **질량**: 무거운 객체는 같은 힘에도 느리게 가속
- **마찰계수**: 얼음(낮음) vs 모래(높음)
- **중력스케일**: 깃털(낮음) vs 돌(높음)
- **최대속도**: 객체별 속도 제한

### 재사용성

```cpp
// Player용 설정
CreateRigidBody();
GetRigidBody()->SetMass(1.f);
GetRigidBody()->SetFriction(10.f);

// Monster용 설정
CreateRigidBody();
GetRigidBody()->SetMass(0.8f);
GetRigidBody()->SetFriction(5.f);
```

## 성능 최적화

### 효율적 연산

- **DeltaTime 기반**: 프레임 독립적 물리 연산
- **조건부 적용**: 중력/마찰을 상황에 맞게만 적용
- **벡터 연산**: 한 번에 2D 벡터 처리

### 메모리 관리

- **컴포넌트 패턴**: 필요한 객체에만 RigidBody 생성
- **friend 접근**: 불필요한 getter/setter 최소화

## 확장 가능성

### 추가 구현 가능한 기능

1. **회전 물리**: 각속도, 토크, 관성 모멘트
2. **충돌 반응**: RigidBody 간 운동량 전달
3. **탄성**: Bounce 계수로 튀어오르는 효과
4. **공기 저항**: 속도에 비례하는 저항력
5. **Joint 시스템**: 객체 간 연결 구조

</details>

---

## 2025-07-27

### 완료

- [x] Tool Scene
  - 오브젝트 배치
  - 오브젝트 삭제
  - 저장/불러오기

<details>
 <summary>Tool Scene (레벨 에디터) 개발 회고</summary>

## 전체 구조 설계

### 기본 접근 방식

- **목표**: 실시간으로 게임 오브젝트를 배치/편집할 수 있는 인게임 레벨 에디터
- **설계 원칙**: 기존 게임 엔진 구조를 최대한 활용하되, 에디터 전용 기능 분리
- **아키텍처**: CScene을 상속받아 게임과 동일한 오브젝트 시스템 사용

## 모드 기반 도구 시스템

### 문제점 인식

- 여러 기능(배치, 선택, 삭제)을 하나의 인터페이스로 통합 필요
- 사용자가 현재 어떤 작업을 하고 있는지 명확히 구분 필요

### 해결 방법

1. **EDITOR_MODE enum 정의**

- NONE: 기본 상태
- PLACE_MONSTER: 몬스터 배치 모드
- SELECT: 오브젝트 선택/이동 모드
- ERASE: 오브젝트 삭제 모드
- CAMERA_MOVE: 카메라 전용 모드 (예약)

2. **모드 전환 시스템**

- Q/W/E/R 키로 모드 간 전환
- ESC 키로 기본 모드 복귀
- 모드 변경 시 윈도우 타이틀과 UI에 현재 상태 표시

## 입력 시스템 통합

### 키 충돌 해결

- **문제점**: 카메라 이동(WASD)과 에디터 모드(W키) 충돌
- **해결책**: 카메라 이동을 화살표 키로 변경
- **결과**: WASD 키를 에디터 기능에 자유롭게 할당 가능

### 마우스 시스템 확장

1. **CKeyMgr에 마우스 기능 추가**

- `KEY::MOUSE_LEFT`, `MOUSE_RIGHT`, `MOUSE_MIDDLE` enum 추가
- 기존 키 상태 시스템(TAP, HOLD, AWAY)을 마우스에도 적용
- `GetMouseWorldPos()` 함수로 월드 좌표 자동 변환

2. **통합 입력 처리**

- `KEY_TAP(KEY::MOUSE_LEFT)` 방식으로 일관된 입력 처리
- 스크린-월드 좌표 변환을 CKeyMgr에서 자동 처리

## 시각적 피드백 시스템

### 실시간 미리보기

1. **배치 모드 미리보기**

- 마우스 위치에 배치될 오브젝트의 반투명 표시
- 해치 패턴과 텍스트로 배치 대상 명시

2. **삭제 모드 미리보기**

- 삭제 대상 오브젝트에 빨간색 X 표시
- "DELETE" 텍스트로 삭제 의도 명확화

3. **선택 시스템**

- 선택된 오브젝트에 노란색 테두리 표시
- 드래그 시 이동 경로를 점선으로 표시
- UI에 선택된 오브젝트의 상세 정보 표시

### 모드별 커서 색상

- PLACE_MONSTER: 녹색 (생성)
- SELECT: 파란색 (선택)
- ERASE: 빨간색 (삭제)
- 기본: 노란색

## 오브젝트 관리 시스템

### 선택 및 조작

1. **AABB 충돌 검사**

- `FindObjectAtPosition()` 함수로 마우스 위치의 오브젝트 탐지
- 오브젝트 크기를 고려한 정확한 선택 영역 계산

2. **드래그 & 드롭**

- 마우스 다운 → 드래그 시작 → 실시간 위치 업데이트 → 마우스 업 시 완료
- 드래그 상태를 UI와 시각적으로 표시

### 삭제 시스템 최적화

- **초기 문제**: 이벤트 시스템의 지연 삭제로 인한 댕글링 포인터 크래시
- **해결책**: 이벤트 삭제 + 즉시 벡터 제거 하이브리드 방식
- **결과**: 메모리 안전성과 즉각적인 UI 반영 양립

## 저장/로딩 시스템

### 데이터 구조 설계

1. **tLevelObjectData 구조체**

- GROUP_TYPE: 오브젝트 분류
- Vec2 vPos, vScale: 위치와 크기
- int iSubType: 향후 확장을 위한 서브 타입

2. **tLevelData 구조체**

- 레벨 메타정보 (이름, 버전)
- 플레이어 스폰 위치
- 오브젝트 배열

### 파일 시스템

1. **바이너리 형식 채택**

- 빠른 읽기/쓰기 성능
- 버전 정보 포함으로 향후 호환성 확보
- .lvl 확장자로 레벨 파일 구분

2. **빠른 저장/로딩**

- F키: quicksave.lvl로 즉시 저장
- V키: quicksave.lvl 즉시 로딩
- 개발 중 빠른 테스트 사이클 지원

## UI 시스템

### 정보 패널 설계

1. **실시간 상태 표시**

- 현재 에디터 모드 (색상으로 강조)
- 오브젝트 개수 통계
- 마우스 월드 좌표
- 선택된 오브젝트 상세 정보

2. **사용자 가이드**

- 모드별 액션 안내 (색상 구분)
- 키 컨트롤 가이드
- 동적 메시지 (클릭, 드래그 상태)

### 확장 가능한 구조

- UI 토글 기능 (H키)
- 패널 크기 자동 조정
- 새로운 정보 추가 시 레이아웃 유연성 확보

## 성과 및 교훈

### 달성한 목표

- ✅ 완전히 작동하는 인게임 레벨 에디터
- ✅ 직관적인 도구 기반 인터페이스
- ✅ 실시간 시각적 피드백
- ✅ 안정적인 저장/로딩 시스템

### 핵심 설계 원칙

1. **기존 시스템 활용**: 게임 엔진의 오브젝트 시스템을 그대로 사용
2. **모드 기반 설계**: 복잡한 기능을 명확한 모드로 분리
3. **즉시 피드백**: 모든 액션에 대한 실시간 시각적/텍스트 피드백
4. **안전성 우선**: 크래시 방지를 위한 방어적 프로그래밍

### 향후 확장 방향

- 오브젝트 팩토리 패턴 도입
- 그리드 시스템 및 스냅 기능
- 언두/리두 시스템
- 다중 선택 및 그룹 편집

</details>

---

### 완료

- [x] 해상도 및 그리드 시스템 구현
  - 원작 160×144에서 4배 확대 640×576으로 변경
- [x] CGrid 클래스 생성 및 구현
- [x] 오브젝트 팩토리 시스템 구현
- [x] CObjectFactory 클래스 생성
- [x] 임시 CItem, CTile, CSpecialObject 클래스 구현
- [x] 에디터 키 매핑 개선 및 충돌 해결
- [x] GROUP_TYPE 확장 (ITEM, TILE, SPECIAL 추가)

### 중요 개념

- 그리드 스냅 시스템
- 팩토리 패턴
- 카테고리별 오브젝트 관리
- 픽셀 퍼펙트 스케일링

<details>
  <summary>개발 회고</summary>

## 전체 구조 개선

### 해상도 스케일링 시스템

- **문제점**: 원작 160×144 해상도는 현대 모니터에 너무 작음
- **해결책**: 4배 확대 (640×576)로 픽셀 퍼펙트 스케일링
- **결과**: 원작 픽셀아트 보존하면서 현대적 플레이 환경 제공

## CGrid (그리드 시스템)

### 그리드 기능

1. **동적 그리드 렌더링**

   - 카메라 위치에 따라 화면에 보이는 부분만 그리기
   - 5배수마다 굵은 선으로 주요 그리드 표시
   - 성능 최적화로 부드러운 카메라 이동

2. **그리드 스냅 시스템**

   - 오브젝트 배치 시 자동으로 그리드에 정렬
   - 드래그 이동 시에도 그리드 스냅 적용
   - 마우스 커서 주변에 그리드 셀 미리보기

3. **유연한 그리드 설정**
   - 1키: 32px (작은 오브젝트용)
   - 2키: 64px (기본 타일) - 메인 그리드
   - 3키: 128px (큰 오브젝트용)
   - 4키: 256px (배경 블록용)

### 커비 게임 최적화

- **기본 타일 크기**: 64×64 (원작 16×16의 4배)
- **그리드 오프셋**: 카메라 이동에 따른 정렬 유지
- **실시간 토글**: G키로 그리드 표시/숨김 가능

## CObjectFactory (오브젝트 팩토리)

### 팩토리 패턴 구현

1. **체계적 오브젝트 관리**

   - OBJECT_TYPE 열거형으로 15가지 오브젝트 타입 정의
   - 카테고리별 분류: Monster, Item, Tile, Special
   - 타입별 기본 크기, 그룹, 속성 자동 설정

2. **커비 게임 전용 오브젝트**

   - **몬스터**: Waddle Dee, Gordos, Bronto Burt, Hot Head
   - **아이템**: Star, Energy Drink, 1UP, Ability Star
   - **타일**: Ground, Spike, Water, Warp Star
   - **특수**: Door, Switch, Mirror

3. **확장 가능한 구조**
   - 새 오브젝트 타입 추가 시 팩토리만 수정
   - 카테고리별 생성 함수로 코드 체계화
   - 유틸리티 함수로 타입 정보 조회 가능

### 에디터 통합

- **카테고리별 배치 모드**: M(몬스터), I(아이템), T(타일), P(특수)
- **오브젝트 선택**: Tab/Shift+Tab으로 카테고리 내 전환
- **스마트 미리보기**: 타입별 색상 구분, 실제 크기 표시

## 키 시스템 개선

### 키 충돌 해결

- **문제점**: T키가 씬 전환과 타일 모드에서 충돌
- **해결책**: Ctrl+T로 씬 전환, T키는 타일 모드 전용
- **결과**: 직관적이고 충돌 없는 키 매핑

### 새로운 키 매핑

1. **오브젝트 배치**

   - M: 몬스터 모드 (Monster)
   - I: 아이템 모드 (Item)
   - T: 타일 모드 (Tile)
   - P: 특수 오브젝트 모드 (sPecial)

2. **편집 도구**

   - S: 선택 모드 (Select) - 더 직관적
   - E: 삭제 모드 (Erase)
   - Tab/Shift+Tab: 오브젝트 종류 전환

3. **파일 & 네비게이션**
   - F: 빠른 저장 (File save)
   - L: 빠른 로드 (Load)
   - Ctrl+T: 게임/에디터 전환

## 추상클래스 문제 해결

### 임시 클래스 구현

1. **CItem 클래스**

   - 아이템 타입, 점수값, 수집 여부 관리
   - Update() 가상함수 구현으로 추상클래스 문제 해결
   - 충돌 시 수집 처리 준비

2. **CTile 클래스**

   - 타일 타입, 충돌 여부, 데미지 여부 관리
   - 정적 타일부터 특수 타일까지 확장 가능

3. **CSpecialObject 클래스**
   - 특수 오브젝트 타입, 활성화 상태 관리
   - 문 열기/닫기, 스위치 토글 등 준비

### GROUP_TYPE 확장

- **기존**: DEFAULT, PLAYER, MONSTER, PROJ_PLAYER, PROJ_MONSTER
- **추가**: ITEM, TILE, SPECIAL, UI
- **결과**: 오브젝트별 충돌 그룹 세분화, 체계적 관리

## UI/UX 개선

### 향상된 에디터 인터페이스

1. **카테고리별 정보 표시**

   - 현재 선택된 오브젝트 이름과 번호
   - 카테고리별 오브젝트 개수 실시간 표시
   - 그리드 설정 상태 확인

2. **컨트롤 가이드 개선**

   - 기능별로 분류된 키 안내
   - 색상으로 구분된 카테고리 표시
   - 직관적인 설명과 단축키 표시

3. **미리보기 시스템**
   - 오브젝트 타입별 색상 구분
   - 실제 크기로 배치 미리보기
   - 그리드 셀 경계 표시

## 메모리 및 성능 최적화

### 효율적 렌더링

- **그리드**: 화면에 보이는 영역만 계산하여 렌더링
- **오브젝트**: 팩토리 패턴으로 불필요한 객체 생성 방지
- **UI**: 토글 가능한 에디터 UI로 성능 절약

### 안전한 메모리 관리

- **팩토리**: 타입별 적절한 클래스 인스턴스 생성
- **그룹 관리**: GROUP_TYPE별 체계적 오브젝트 분류
- **임시 클래스**: 추상클래스 문제 해결로 안전한 객체 생성

</details>

---

## 2025-08-01

### 완료

- [x] Background, Tile 매니저 추가

<details>
  <summary>개발 회고</summary>

## 전체 구조 개선

### 메시지 루프 변경

- **문제점**: `GetMessage()` 함수는 메시지 큐가 비어있는 시간이 많아 비효율적
- **해결책**: `PeekMessage()` 함수로 변경하여 메시지가 없는 시간을 활용
- **결과**: 메시지가 없을 때 `CCore::GetInst()->progress()` 함수를 지속적으로 호출

## CCore (핵심 엔진)

### 초기화 과정

1. 메시지 루프 전에 CCore 싱글톤 객체 생성
2. 메인 윈도우 정보 전달
   - 윈도우 핸들 (hWnd)
   - 윈도우 크기 (ptResolution: 1920x1080)
3. 윈도우 크기를 해상도에 맞게 조정
4. 더블 버퍼링을 위한 비트맵과 메모리 DC 생성
5. 각 매니저 싱글톤 객체 생성 및 초기화 (순서 중요)
   - CTimeMgr, CKeyMgr, CPathMgr, CResMgr
   - CBackgroundMgr, CTileMgr (새로 추가)
   - CSceneMgr, CCollisionMgr, CEventMgr
   - CCamera, CGrid

### Progress 함수

- Update: 각 매니저 업데이트 → 씬 업데이트 → 충돌 처리 → 이벤트 처리
- Render: 메모리 DC에 렌더링 → 메인 DC로 BitBlt → FPS 표시

## CTimeMgr (시간 관리자)

### 문제점

- 컴퓨터 성능에 따라 함수 호출 빈도가 달라짐
- 같은 시간에도 성능에 따라 처리량이 다름

### 해결 방법

1. **고해상도 타이머 사용**

   - `QueryPerformanceCounter()`: 현재 카운트 값
   - `QueryPerformanceFrequency()`: 초당 카운트 횟수
   - 정밀한 시간 측정 가능

2. **Delta Time (DT) 계산**

   - 이전 프레임과 현재 프레임 간의 시간차 계산
   - 모든 움직임과 애니메이션에 DT 곱하여 프레임 독립적 동작

3. **추가 기능**
   - FPS 계산 및 윈도우 타이틀 표시
   - 디버그 모드에서 최대 60FPS 제한

## CKeyMgr (키 입력 관리자)

### 문제점

- Windows의 `GetAsyncKeyState()`는 현재 상태만 제공
- 키 이벤트(방금 눌림, 방금 뗌) 감지 어려움

### 해결 방법

1. **4단계 키 상태 시스템**

   - NONE: 이전/현재 모두 안눌림
   - TAP: 이전 안눌림 → 현재 눌림 (방금 눌림)
   - HOLD: 이전/현재 모두 눌림 (계속 누르고 있음)
   - AWAY: 이전 눌림 → 현재 안눌림 (방금 뗌)

2. **구현 내용**

   - 모든 키에 대한 상태 추적 (`m_vecKey`)
   - 윈도우 포커스 상태 처리 (포커스 잃으면 모든 키 AWAY)
   - 마우스 월드 좌표 변환 기능

3. **확장된 키 지원**
   - 방향키, 알파벳, 숫자키, 특수키
   - 마우스 좌/우/중간 버튼

## CSceneMgr (씬 관리자)

### 씬 시스템 구조

1. **3개 씬으로 구성**

   - CScene_Start: 시작 화면
   - CScene_Tool: 레벨 에디터
   - CScene_Stage01: 실제 게임 스테이지

2. **씬 전환 시스템**
   - 이벤트 기반 씬 변경
   - Enter()/Exit() 가상함수로 초기화/정리
   - Ctrl+T로 게임↔에디터 전환

## CObject & 상속 구조

### 기본 오브젝트 시스템

- **CObject**: 모든 게임 오브젝트의 기본 클래스
  - 위치(Vec2), 크기(Vec2), 텍스처 정보
  - 컴포넌트 시스템: CCollider, CAnimator, CRigidBody

### 게임 오브젝트들

1. **CPlayer**: 커비 캐릭터

   - 상태 기반 애니메이션 (IDLE, WALK, JUMP)
   - 물리 기반 이동 및 점프
   - 타일과의 충돌 처리

2. **CMonster**: AI 기반 몬스터 (웨들디)

   - FSM 상태 머신 (IDLE, WALK, TURN)
   - 벽/절벽 감지 및 방향 전환
   - 플레이어와 동일한 물리 시스템

3. **CTile**: 다양한 지형 타일
   - 기능적 타입 vs 시각적 타입 분리
   - 장식용/충돌용/데미지용 구분
   - 텍스처 기반 렌더링 + 폴백 색상

## 애니메이션 시스템

### CAnimation & CAnimator

1. **프레임 기반 애니메이션**

   - 스프라이트 시트에서 프레임 정보 추출
   - 프레임별 지속 시간 설정
   - 루프/논루프 애니메이션 지원

2. **고급 애니메이션 기능**
   - 커스텀 애니메이션 생성 (커비 IDLE)
   - 복잡한 타이밍 제어 (깜빡임 효과)
   - 마젠타(RGB(255,0,255)) 투명 처리

## 물리 시스템

### CRigidBody 컴포넌트

1. **물리 법칙 적용**

   - 뉴턴 역학: F=ma, v=v0+at, s=s0+vt
   - 중력, 마찰력, 최대 속도 제한
   - 지면 감지 및 상태 관리

2. **실용적 튜닝**
   - 플레이어: 빠른 반응성, 정확한 점프
   - 몬스터: 안정적인 AI 움직임
   - 타일과의 정밀한 충돌 처리

## 충돌 시스템

### CCollider & CCollisionMgr

1. **AABB 충돌 감지**

   - 사각형 기반 빠른 충돌 검사
   - 그룹별 충돌 매트릭스 관리
   - Enter/Stay/Exit 이벤트 시스템

2. **정밀한 충돌 처리**
   - 플레이어 착지 시 정확한 위치 보정
   - 벽 충돌 시 관통 방지
   - 일방향 플랫폼 구현

## 카메라 시스템

### CCamera

1. **부드러운 추적**

   - 플레이어 자동 추적
   - 보간을 통한 부드러운 움직임
   - 수동 카메라 조작 (WASD)

2. **특수 효과**
   - 화면 흔들림 (몬스터 충돌 시)
   - 월드↔스크린 좌표 변환
   - 렌더링 컬링 최적화

## 레벨 에디터 시스템

### CScene_Tool의 고급 기능

1. **모드 기반 편집**

   - 배치 모드: Monster(M), Item(I), Tile(T), Special(P)
   - 배경 모드: Background(B) - 실시간 배경 변경
   - 편집 모드: Select(S), Erase(E)

2. **사용자 친화적 인터페이스**

   - 450x800 픽셀 UI 패널
   - 실시간 정보 표시 (모드, 오브젝트 수, 단축키)
   - 마우스 커서 모드별 시각화
   - 배치 미리보기 시스템

3. **그리드 시스템**
   - 4단계 그리드 크기 (32, 64, 128, 256px)
   - 그리드 스냅 기능
   - 주요 그리드선 강조 표시

## 리소스 관리 시스템

### CResMgr, CBackgroundMgr, CTileMgr

1. **통합 텍스처 관리**

   - 중복 로드 방지
   - 키 기반 빠른 검색
   - 메모리 효율적 관리

2. **배경 시스템**

   - 무한 스크롤링 배경
   - 패럴랙스 효과
   - 실시간 배경 전환

3. **타일 시스템**
   - 시각적/기능적 타입 분리
   - 자동 크기 및 속성 설정
   - 다양한 크기 지원 (32x32 ~ 128x96)

## 이벤트 시스템

### CEventMgr

1. **지연 실행 이벤트**

   - 오브젝트 생성/삭제를 프레임 말에 처리
   - 씬 전환 이벤트
   - 충돌 Enter/Exit 이벤트

2. **메모리 안전성**
   - 가비지 컬렉션 시스템
   - 안전한 오브젝트 삭제
   - 댕글링 포인터 방지

## 파일 시스템

### 레벨 저장/로드

1. **바이너리 파일 형식**

   - 버전 호환성 지원 (v1, v2)
   - 배경 정보 저장 (v2 추가)
   - 타일 시각 타입 정보 저장

2. **사용자 편의 기능**
   - 빠른 저장/로드 (F/L)
   - 파일 대화상자 (Ctrl+S/O)
   - 기본 레벨 자동 생성

## 성능 최적화

### 렌더링 최적화

1. **더블 버퍼링**

   - 화면 깜빡임 제거
   - 부드러운 애니메이션

2. **효율적 렌더링**
   - 카메라 컬링
   - 조건부 렌더링 (디버그 정보)
   - 투명 처리 최적화

### 메모리 관리

1. **싱글톤 패턴**

   - 매니저 클래스들의 유일성 보장
   - 전역 접근 편의성

2. **RAII 원칙**
   - 생성자/소멸자에서 리소스 관리
   - 스마트 포인터 대신 명시적 메모리 관리

## 확장성 고려사항

### 모듈화된 설계

1. **컴포넌트 시스템**

   - 애니메이터, 충돌체, 물리체를 독립적으로 추가/제거
   - 새로운 컴포넌트 쉽게 확장 가능

2. **팩토리 패턴**

   - CObjectFactory로 오브젝트 생성 통합
   - 새로운 오브젝트 타입 쉽게 추가

3. **이벤트 기반 설계**
   - 느슨한 결합으로 시스템 간 통신
   - 새로운 이벤트 타입 확장 용이

## 향후 개선 방향

### 기능 확장

1. **사운드 시스템**: BGM, 효과음 재생
2. **UI 시스템**: 게임 내 메뉴, HUD
3. **세이브 시스템**: 게임 진행 상황 저장
4. **능력 시스템**: 커비의 다양한 능력

### 성능 개선

1. **스프라이트 배칭**: 동일 텍스처 일괄 렌더링
2. **오브젝트 풀링**: 오브젝트 생성/삭제 최적화
3. **멀티스레딩**: 물리 계산, 사운드 등 병렬 처리

### 개발 도구

1. **애니메이션 에디터**: 복잡한 애니메이션 제작
2. **충돌 편집기**: 정밀한 충돌 영역 설정
3. **스크립트 시스템**: 게임 로직의 유연한 제어

</details>

---

### 완료

- [x] ToolScene 플레이어 스폰 위치 기능 추가

## 2025-08-02

### 완료

- [x] 커비 IDLE 애니메이션 수정
- [x] 좌우 상태에 따라 스프라이트 뒤집기
- [x] WALK에서 IDLE로 돌아올 때 관성 추가
- [x] Tool Scene의 기능을 여러 클래스로 분리

## 2025-08-03

### 완료

- [x] CCamera의 update 함수를 여러 함수로 분리
- [x] CSceneMgr의 ChangeScene 함수의 접근 제어자를 기존 public에서 private으로 변경, CEventMgr 을 Friend class 로 추가
- [X] CPlayer 디버깅코드 제거
- [X] Flip 애니메이션 단순화
- [X] CPlayer StateMachine 분리
- [X] CPlayer InhaleSystem 분리
- [X] CPlayer Movement 분리
- [X] 착지 후 착지를 반복하던 현상 수정

## 2025-08-04

### 완료

- [X] ToolScene에 UI 추가
- [X] 체력 시스템과 게임오버 추가
- [X] 레벨 에디터에서 오브젝트 팔레트가 안 바뀌던 오류 수정


## 2025-08-05

### 완료

- [X] ToolScene에 카메라 범위 설정
- [X] 레벨 에디터에 맵 범위 설정 추가
- [X] 레벨 에디터 메인UI 제거(주석처리)
- [X] 문 오브젝트 추가

## 2025-08-06

### 완료

- [X] 스프라이트 작업

## 2025-08-07

### 완료

- [X] 기존 마젠타 방식에서 알파채널을 이용하는 방식으로 변경
- [X] 오류가 많아 다시 마젠타 방식으로 변경

## 2025-08-08

### 완료

- [X] 스프라이트 작게 하고 4배 스케일 적용
- [X] 툴씬과 게임 씬 해상도 분리
- [X] 툴씬 좌표계 변경

## 2025-08-09

### 완료

- [X] CCore 클래스 리팩토링
  - 헤더의 멤버들 순서 정리
  - ChangeResolution 함수 책임 분리
  - 코드 블럭 정리
  - ChangeREsolution 접근제어자 변경
- [X] CSceneMgr 클래스 리팩토링
  - 헤더의 멤버들 순서 정리
  - update에서 핫 키 기능 책임 분리
  - O(N)의 GetCurSceneType함수 O(1)로 최적화
  - 안정성 검사 조건문 추가
  - Getter 함수에 const 키워드 추가
- [X] CScene, CScene_Tool 리팩토링
  - 컨벤션에 맞게 수정
- [X] CEditorCore 렌더링
  - CScene_Tool에 있던 중복 렌더링 삭제
- [X] CStageMgr 리팩토링

## 2025-08-10

### 완료

- [X] CCStageMgr 리팩토링 완료
- [X] CStageImage 리팩토링 완료 (4배 스케일링 적용)
- [X] CEditorCameraController 리팩토링 완료 (헤더 구조, 주석 정리, 디버깅 코드 제거, 불필요한 기능 제거)
  
## 2025-08-11

### 완료

- [X] CObject 코드 리팩토링
- [X] CAnimator, CAnimation 코드 리팩토링
  - 알파 채널 이미지 처리 기능 제거

## 2025-08-12

### 완료

- [X] CPlayer 코드 리팩토링
  - CPlayerColliderManager 추가

- ## 2025-08-14

### 완료

- [X] 레벨 에디터 간소화
- [X] 애니메이션 json파일 불러오기로 변경
- [X] 몬스터 상속 구조
- [X] 빨아들이기 시스템, 이벤트 종류 추가

- ## 2025-08-15

### 완료

- [X] 웨이들 디 구현
- [X] 플레이어 기본 움직임 구현