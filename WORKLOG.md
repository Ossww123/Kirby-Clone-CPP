# Work Log

## 2025-07-19
### 완료
- [X] 메세지 큐 구조 변경
  - GetMessage()에서 PeekMessage()로 변경
- [X] Core 클래스 생성
  - 싱글톤 패턴
- [X] CKeyMgr 클래스 생성
- [X] define.h, pch.h(미리 컴파일된 헤더) 추가
- [X] 싱글톤 매크로 추가
- [X] CCore 클래스 init 추가

### 중요 개념
- 매크로
- 싱글톤

## 2025-07-20
### 완료
- [X] CObject 클래스 생성

## 2025-07-23
### 완료
- [X] CTimeMgr 클래스 생성 및 구현
- [X] double buffering 테스트
- [X] 키 상태, 키값 매핑을 위한 enum.h 생성
- [X] CKeyMgr 클래스 구현
  - 목적 : 윈도우에서 제공하는 함수는 현재 상태만 알 수 있어서 이벤트 감지가 어려움

### 중요 개념
- 시간 동기화
- 더블 버퍼링

<details>
  <summary>개발 회고</summary>

## 전체 구조 개선

### 메시지 루프 변경
- **문제점**: `GetMessage()` 함수는 메시지 큐가 비어있는 시간이 많아 비효율적
- **해결책**: `PeekMessage()` 함수로 변경하여 메시지가 없는 시간을 활용
- **결과**: 메시지가 없을 때 `CCore::GetInst()->progress()` 함수를 지속적으로 호출

## CCore (핵심 엔진)

### 초기화 과정
1. 메시지 루프 전에 CCore 싱글톤 객체 생성
2. 메인 윈도우 정보 전달
   - 윈도우 핸들 (hWnd)
   - 윈도우 크기 (ptResolution)
3. 윈도우 크기를 버전에 맞게 조정
4. 더블 버퍼링을 위한 비트맵 생성
5. 각 매니저 싱글톤 객체 생성 및 초기화
6. 임시 글로벌 오브젝트 생성

### Progress 함수
- 각 매니저 업데이트
- 임시 오브젝트(g_obj) 업데이트
- 렌더링 처리

### 현재 오브젝트 구조
- 단순히 위치(Vec2)와 크기(Vec2)만 보유

## CTimeMgr (시간 관리자)

### 문제점
- 컴퓨터 성능에 따라 함수 호출 빈도가 달라짐
- 같은 시간에도 성능에 따라 처리량이 다름

### 해결 방법
1. **초기화**
   - 이전 카운트 값 (m_llPrevCount) 저장
   - 초당 카운트 횟수 (m_llFrequency) 저장

2. **업데이트**
   - 현재 카운트 값 (m_llCurCount) 측정
   - 한 프레임당 시간 (m_dDT) 계산
   - DT를 연산에 곱하여 프레임 독립적인 동작 구현

3. **추가 기능**
   - 윈도우 타이틀에 FPS 정보 표시

## CKeyMgr (키 입력 관리자)

### 문제점
- Windows의 `GetAsyncKeyState()`는 현재 상태만 제공
- 키 이벤트(방금 눌림, 방금 뗌) 감지 어려움

### 해결 방법
1. **키 상태 정의**
   - NONE: 이전에도 안눌림, 지금도 안눌림
   - TAP: 이전에 안눌림, 지금 눌림
   - HOLD: 이전에도 눌림, 지금도 눌림
   - AWAY: 이전에 눌림, 지금 안눌림

2. **구현 내용**
   - `m_vecKey` 벡터에 각 키 정보 저장
   - 키 상태와 이전 프레임 눌림 여부 추적
   - 윈도우 포커싱 상태 처리
   - 매 프레임마다 모든 키 상태 업데이트

3. **사용 방법**
   - CKeyMgr 싱글톤의 멤버 함수 호출로 키 상태 확인
   - `IsKeyTap()`, `IsKeyHold()`, `IsKeyAway()` 등 편의 함수 제공

</details>

### 완료
- [X] CScene 클래스 생성
- [X] CObject Update와 Render 추가
- [X] Player, Monster 오브젝트 추가 
- [X] Vec2 연산자 오버로딩
- [X] Scene 전환 시 사용할 DeleteAllObject 함수
- [X] Collider 컴포넌트와 매니저 추가

### 중요 개념
- 순수 가상 함수 - 자식 클래스는 이 함수를 반드시 구현해야 한다.
- 자식 클래스의 소멸자를 호출해야 하기 때문에 소멸자에 'virtual' 키워드 필요
- 컴포넌트 구조 - 클래스 상속만으로는 유동성이 부족

<details>
  <summary>개발 회고</summary>

## 전체 구조 개선

### 게임 아키텍처 설계
- **문제점**: 단일 전역 오브젝트(g_obj)로는 복잡한 게임 구현 불가
- **해결책**: Scene 시스템과 Object 상속 구조 도입
- **결과**: 상황별로 다른 오브젝트들을 효율적으로 관리 가능

## CScene & CSceneMgr (씬 시스템)

### Scene 구조
1. **기본 Scene 클래스**
   - 오브젝트들을 그룹별로 관리 (m_arrObj)
   - Enter/Exit 가상 함수로 씬 전환 시 초기화/정리
   - Update/Render로 모든 오브젝트 일괄 처리

2. **Scene Manager**
   - 싱글톤 패턴으로 구현
   - 모든 씬을 배열로 관리
   - T키로 씬 전환 기능 구현

3. **구현된 씬**
   - CScene_Start: 플레이어 1개, 몬스터 1개
   - CScene_Tool: 플레이어 1개, 몬스터 5개 (테스트용)

### 메모리 관리
- **문제점**: 씬 전환 시 오브젝트 메모리 누수 발생
- **해결책**: Exit() 함수에서 DeleteAllObject() 호출
- **결과**: 씬 전환 시 안전한 메모리 정리

## CObject (오브젝트 시스템)

### 상속 구조
1. **기본 Object 클래스**
   - 위치(Vec2), 크기(Vec2), 생존 여부(bool) 보유
   - Update()는 순수 가상 함수로 자식이 구현
   - Render()는 기본 사각형 렌더링 제공
   - 가상 소멸자로 안전한 다형성 보장

2. **Player 클래스**
   - 키 입력 처리 (방향키 이동, 스페이스 리셋)
   - Update()에서 이동 로직 구현

3. **Monster 클래스**
   - 자동 좌우 왕복 이동
   - 중심점 기준 최대 거리까지 이동 후 방향 전환

## CCollider & CCollisionMgr (충돌 시스템)

### Collider 구조
1. **충돌체 컴포넌트**
   - 오브젝트와 별도로 충돌 영역 관리
   - 오프셋과 크기로 충돌 박스 정의
   - 고유 ID로 충돌 쌍 식별
   - 디버그용 초록색 박스 렌더링

2. **충돌 검사**
   - AABB(Axis-Aligned Bounding Box) 방식
   - 사각형 간 겹침 여부로 충돌 판정

### CollisionMgr 구조
1. **그룹별 충돌 관리**
   - GROUP_TYPE으로 오브젝트 분류 (PLAYER, MONSTER 등)
   - 비트 연산으로 충돌 체크할 그룹 쌍 설정
   - 그룹 간 모든 오브젝트 충돌 검사

2. **충돌 상태 추적**
   - map으로 충돌 쌍의 이전 상태 저장
   - Enter(충돌 시작), Stay(충돌 중), Exit(충돌 끝) 구분
   - 각 상태별로 다른 콜백 함수 호출

3. **충돌 콜백 시스템**
   - Object 클래스에 가상 함수로 정의
   - OnCollisionEnter/OnCollision/OnCollisionExit
   - 자식 클래스에서 필요시 재정의

## 유틸리티 개선

### Vec2 구조체 확장
- **추가된 연산자**: +, -, *, /, +=, -=, *=, /=, ==, !=
- **유틸리티 함수**: Length(), Normalize()
- **결과**: 벡터 연산 코드 가독성 대폭 향상

## 향후 계획
- **별의 커비 구현**을 위한 추가 기능
  - 적 흡수 메카닉
  - 능력 복사 시스템
  - 공중 부양
  - 투사체 시스템

</details>

## 2025-07-24
### 완료
- [X] 이벤트 매니저 생성
- [X] 이벤트 관련 매크로 추가

### 중요 개념

<details>
  <summary>이벤트 매니저 개발 회고</summary>

## 이벤트 매니저 도입 배경

### 기존 시스템의 문제점
- **동시성 문제**: 충돌 시 즉시 처리로 인한 예측 불가능한 실행 순서
- **Update 중 삭제 위험**: 오브젝트 순회 중 삭제 시 크래시 발생 가능
- **씬 전환 타이밍**: T키 입력 시 즉시 씬 전환으로 인한 불안정성
- **확장성 부족**: 새로운 이벤트 추가 시 각 클래스마다 개별 처리 필요

### 해결 목표
- 한 프레임 내 모든 이벤트의 동시성 보장
- 안전한 오브젝트 생명주기 관리
- 확장 가능한 이벤트 시스템 구축

## CEventMgr 아키텍처

### 핵심 설계 원칙
1. **지연 처리 (Deferred Execution)**
   - 이벤트 발생 시 즉시 실행 X → 큐에 저장
   - 프레임 마지막에 일괄 처리
   - 실행 순서 보장 및 안정성 확보

2. **이벤트 타입 시스템**
   ```cpp
   enum class EVENT_TYPE
   {
       CREATE_OBJECT,    // 오브젝트 생성
       DELETE_OBJECT,    // 오브젝트 삭제  
       SCENE_CHANGE,     // 씬 변경
       COLLISION_ENTER,  // 충돌 시작
       COLLISION_EXIT,   // 충돌 종료
   }
   ```

3. **가비지 컬렉션**
   - 삭제 예정 오브젝트를 즉시 삭제하지 않고 다음 프레임에 처리
   - Update 도중 오브젝트 삭제로 인한 메모리 오류 방지

### 구현 세부사항
1. **이벤트 구조체**
   ```cpp
   struct tEvent
   {
       EVENT_TYPE eType;    // 이벤트 종류
       DWORD_PTR wParam;    // 첫 번째 매개변수
       DWORD_PTR lParam;    // 두 번째 매개변수
   }
   ```

2. **실행 순서**
   - ClearGarbageObject() → 이전 프레임 삭제 대상 정리
   - Execute() → 현재 프레임 이벤트 처리
   - m_vecEvent.clear() → 처리된 이벤트 클리어

## 기존 시스템 통합

### 1. 충돌 시스템 개선
**변경 전**: 충돌 즉시 콜백 호출
```cpp
pLeftCol->OnCollisionEnter(pRightCol);  // 즉시 실행
```

**변경 후**: 이벤트로 지연 처리
```cpp
tEvent event(EVENT_TYPE::COLLISION_ENTER, (DWORD_PTR)pLeftCol, (DWORD_PTR)pRightCol);
CEventMgr::GetInst()->AddEvent(event);  // 큐에 저장
```

**결과**: OnCollision은 매 프레임 호출되어야 하므로 즉시 처리 유지

### 편의성 향상
**매크로 정의**로 사용성 개선:
```cpp
#define CREATE_OBJECT(obj, group) // 오브젝트 생성 이벤트
#define DELETE_OBJECT(obj)        // 오브젝트 삭제 이벤트  
#define CHANGE_SCENE(scene)       // 씬 변경 이벤트
```

## 개선 효과

### 안정성 향상
- **메모리 안전성**: Update 중 오브젝트 삭제 시 크래시 방지
- **예측 가능한 실행**: 모든 이벤트가 정해진 순서로 처리
- **상태 일관성**: 프레임 내 모든 객체가 동일한 상태 기준으로 처리


</details>

### 완료
- [X] 카메라 기능 생성

<details>
  <summary>카메라 시스템 개발 회고</summary>

## 카메라 시스템 도입 배경

### 기존 시스템의 한계점
- **고정된 시점**: 모든 오브젝트가 화면 해상도 내에서만 활동
- **제한된 월드 크기**: 1280x768 화면 크기가 곧 게임 월드 전체
- **시각적 단조로움**: 정적인 화면으로 인한 몰입도 저하
- **확장성 부족**: 큰 맵이나 다양한 연출 효과 구현 불가

### 해결 목표
- 큰 게임 월드와 작은 화면의 분리
- 동적이고 생동감 있는 카메라 연출
- 플레이어 추적 및 자동 카메라 기능
- 타격감을 위한 화면 효과 시스템

## CCamera 아키텍처 설계

### 핵심 설계 원칙
   **좌표 시스템 분리**
   ```cpp
   // 월드 좌표: 오브젝트가 실제 존재하는 위치 (게임 로직용)
   Vec2 worldPos = player->GetPos();
   
   // 화면 좌표: 실제로 그려지는 위치 (렌더링용)  
   Vec2 screenPos = CCamera::GetInst()->GetRenderPos(worldPos);
   ```

### 좌표 변환 시스템
1. **변환 공식**
   ```cpp
   m_vDiff = m_vCurLookAt - (해상도 / 2);
   화면좌표 = 월드좌표 - m_vDiff;
   월드좌표 = 화면좌표 + m_vDiff;
   ```

2. **CalDiff() 함수의 역할**
   - 화면 중심과 카메라 시점 간의 차이값 계산
   - 모든 오브젝트 렌더링 시 이 차이값을 적용

### 충돌 시스템 호환성
- **충돌 계산**: 월드 좌표에서 수행 (카메라 영향 없음)
- **충돌체 렌더링**: 화면 좌표로 변환하여 표시
- **일관성 유지**: 게임 로직과 시각적 표현의 분리

</details>

## 2025-07-25
### 완료
- [X] Path 매니저, Resource 매니저 추가
- 빌드 경로 변경

### 중요 개념
- 매니저 초기화 순서 주의


<details>
  <summary>리소스 매니저 개발 회고</summary>

## CPathMgr (경로 관리자)

### 문제점
- 리소스 파일들의 경로를 하드코딩하면 프로젝트 구조 변경 시 문제 발생
- 개발 환경과 배포 환경에서 상대 경로가 달라질 수 있음
- 각 클래스에서 개별적으로 경로를 처리하면 관리가 어려움

### 해결 방법
1. **경로 중앙화**
   - 모든 리소스 경로를 하나의 매니저에서 관리
   - Content 폴더를 기준으로 한 상대 경로 시스템 구축

2. **동적 경로 계산**
   - `GetCurrentDirectory()`로 실행 파일의 현재 작업 디렉토리 획득
   - `bin` 폴더 구조를 기반으로 Content 폴더까지의 절대 경로 계산
   - `m_strContentPath`에 "프로젝트루트\\bin\\content\\" 경로 저장

3. **상대 경로 지원**
   - `GetRelativePath()` 함수로 절대 경로에서 Content 경로 부분 제거
   - 리소스의 상대 경로만으로 파일 관리 가능

### 폴더 구조
```
KirbyClone/
├── bin/
│   ├── content/          ← 모든 리소스 파일
│   │   ├── texture/
│   │   ├── sound/
│   │   └── data/
│   └── x64/
│       ├── Debug/        ← 실행 파일 위치
│       └── Release/
```

## CResMgr (리소스 관리자)

### 문제점
- 같은 리소스를 여러 번 로드하면 메모리 낭비
- 리소스 파일 경로 관리의 복잡성
- 리소스 해제 시점 관리 어려움

### 해결 방법
1. **리소스 캐싱 시스템**
   - `map<wstring, CRes*> m_mapTex`로 로드된 텍스처 관리
   - 키(문자열)를 통한 리소스 식별 및 재사용
   - `FindTexture()` 함수로 이미 로드된 리소스 확인

2. **자동 경로 처리**
   - CPathMgr과 연동하여 상대 경로를 절대 경로로 변환
   - `LoadTexture(키, 상대경로)` 형태로 간편한 인터페이스 제공

3. **메모리 관리**
   - 소멸자에서 모든 리소스 자동 해제
   - 중복 로드 방지로 메모리 효율성 향상

### 사용 예시
```cpp
// 텍스처 로드 (한 번만 로드됨)
CTexture* pTex = CResMgr::GetInst()->LoadTexture(L"Player", L"texture\\Player.bmp");

// 같은 키로 다시 호출 시 기존 텍스처 반환 (재로드 없음)
CTexture* pSameTex = CResMgr::GetInst()->LoadTexture(L"Player", L"texture\\Player.bmp");
```

## CRes & CTexture (리소스 기반 클래스)

### 설계 구조
1. **CRes (추상 기반 클래스)**
   - 모든 리소스의 공통 속성 정의
   - 키값(m_strKey)과 상대경로(m_strRelativePath) 관리
   - 확장성을 위한 가상 소멸자 제공

2. **CTexture (구체 클래스)**
   - CRes를 상속받아 비트맵 리소스 전용 기능 구현
   - HDC, HBITMAP을 통한 Windows GDI 연동
   - `Load()` 함수에서 HRESULT 반환으로 오류 처리

### 주요 개선 사항
- **초기화 순서 해결**: CPathMgr, CResMgr를 CSceneMgr보다 먼저 초기화
- **오류 처리**: 파일 로드 실패 시 적절한 메시지와 함께 E_FAIL 반환
- **호환성**: CObject와 연동하여 기존 사각형 렌더링과 텍스처 렌더링 선택 가능

### 확장성
- 향후 사운드(CSound), 애니메이션(CAnimation) 등 다른 리소스 타입 추가 용이
- 리소스 타입별 전용 맵 구조로 확장 가능한 설계
</details>

### 완료
- [X] FSM과 이에 따른 애니메이션 추가

### 중요 개념
- FSM

<details>
  <summary>애니메이션 & FSM 개발 회고</summary>

## 애니메이션 시스템 구현

### CAnimation (개별 애니메이션 관리)
- **목적**: 스프라이트 시트에서 프레임별 애니메이션 데이터 관리
- **구조**:
  - `tAnimFrame`: 각 프레임의 좌표, 크기, 지속시간 정보
  - `m_vecFrame`: 애니메이션을 구성하는 프레임들의 벡터
  - `m_iCurFrame`: 현재 재생 중인 프레임 인덱스
  - `m_fAccTime`: 프레임 전환을 위한 누적 시간

### 핵심 기능
1. **Create()**: 스프라이트 시트에서 연속된 프레임들을 자동 생성
   - 시작 좌표, 프레임 크기, 간격, 지속시간, 프레임 수 설정
2. **Update()**: DeltaTime 기반 프레임 전환 처리
3. **Render()**: BitBlt를 사용한 프레임별 렌더링
4. **Reset()**: 애니메이션 처음부터 재시작

## CAnimator (애니메이션 관리자)

### 설계 목적
- **문제점**: 오브젝트마다 여러 애니메이션을 개별 관리하기 복잡
- **해결책**: 하나의 컴포넌트로 모든 애니메이션을 통합 관리

### 구현 내용
1. **애니메이션 저장소**
   - `map<wstring, CAnimation*>`: 이름으로 애니메이션 관리
   - 동적 생성/삭제 및 안전한 메모리 관리

2. **재생 제어**
   - `Play()`: 지정된 애니메이션으로 전환
   - 반복 재생 여부 설정 가능
   - 자동 리셋으로 부드러운 전환

3. **Owner 연결**
   - `friend class CObject`로 안전한 소유권 관리
   - 렌더링 시 소유자 오브젝트 위치 자동 참조

## FSM (Finite State Machine) 구현

### 상태 정의
```cpp
enum class PLAYER_STATE
{
    IDLE,   // 대기 상태
    WALK,   // 걷기 상태  
    JUMP,   // 점프 상태
    END
};
```

### 상태 전환 로직
1. **입력 기반 상태 결정**
   - 바닥 여부 우선 확인 (점프 상태)
   - 이동 입력 확인 (걷기 상태)
   - 기본값은 대기 상태

2. **상태 변경 시 처리**
   - `ChangeState()`: 이전 상태 저장 후 새 상태로 전환
   - 상태별 애니메이션 자동 재생
   - 반복/비반복 설정 (점프는 1회, 나머지는 반복)

## 물리 시스템과의 연동

### 중력 및 점프 구현
- **중력**: 매 프레임 `m_fGravity * DT`만큼 Y 속도 증가
- **점프**: 스페이스바 + 바닥 접촉 시에만 점프력 적용
- **바닥 충돌**: 간단한 Y좌표 기반 바닥 감지

### 상태와 물리의 동기화
- 물리 상태(바닥 접촉, 이동 속도)가 FSM 상태 결정
- FSM 상태가 애니메이션 선택
- 일관된 캐릭터 동작 보장

## 메모리 관리 개선

### 소멸자 안전성 확보
- **문제점**: 씬 전환 시 iterator 접근 위반 에러
- **해결책**: 
  ```cpp
  // 안전한 map 순회 및 삭제
  for (auto iter = m_mapAnim.begin(); iter != m_mapAnim.end(); )
  {
      delete iter->second;
      iter = m_mapAnim.erase(iter);
  }
  ```

### Component 패턴 적용
- CObject가 CAnimator를 소유하는 구조
- `CreateAnimator()`: 부모 클래스에서 안전한 생성
- `GetAnimator()`: 접근자 함수로 캡슐화 유지

## 확장 가능한 구조

### 애니메이션 확장성
- 스프라이트 시트 좌표만 수정하면 새 애니메이션 추가 가능
- 프레임별 지속시간 개별 설정 가능
- 반복/비반복 애니메이션 지원

### FSM 확장성
- 새로운 상태 추가 시 enum과 switch문만 수정
- 상태별 진입/종료 로직 추가 가능
- 복잡한 상태 조건 설정 가능

</details>
