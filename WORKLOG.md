# Work Log

## 2025-07-19
### 완료
- [X] 메세지 큐 구조 변경
  - GetMessage()에서 PeekMessage()로 변경
- [X] Core 클래스 생성
  - 싱글톤 패턴
- [X] CKeyMgr 클래스 생성
- [X] define.h, pch.h(미리 컴파일된 헤더) 추가
- [X] 싱글톤 매크로 추가
- [X] CCore 클래스 init 추가

### 중요 개념
- 매크로
- 싱글톤

## 2025-07-20
### 완료
- [X] CObject 클래스 생성

## 2025-07-23
### 완료
- [X] CTimeMgr 클래스 생성 및 구현
- [X] double buffering 테스트
- [X] 키 상태, 키값 매핑을 위한 enum.h 생성
- [X] CKeyMgr 클래스 구현
  - 목적 : 윈도우에서 제공하는 함수는 현재 상태만 알 수 있어서 이벤트 감지가 어려움

### 중요 개념
- 시간 동기화
- 더블 버퍼링

<details>
  <summary>여기까지 개발 회고</summary>

## 전체 구조 개선

### 메시지 루프 변경
- **문제점**: `GetMessage()` 함수는 메시지 큐가 비어있는 시간이 많아 비효율적
- **해결책**: `PeekMessage()` 함수로 변경하여 메시지가 없는 시간을 활용
- **결과**: 메시지가 없을 때 `CCore::GetInst()->progress()` 함수를 지속적으로 호출

## CCore (핵심 엔진)

### 초기화 과정
1. 메시지 루프 전에 CCore 싱글톤 객체 생성
2. 메인 윈도우 정보 전달
   - 윈도우 핸들 (hWnd)
   - 윈도우 크기 (ptResolution)
3. 윈도우 크기를 버전에 맞게 조정
4. 더블 버퍼링을 위한 비트맵 생성
5. 각 매니저 싱글톤 객체 생성 및 초기화
6. 임시 글로벌 오브젝트 생성

### Progress 함수
- 각 매니저 업데이트
- 임시 오브젝트(g_obj) 업데이트
- 렌더링 처리

### 현재 오브젝트 구조
- 단순히 위치(Vec2)와 크기(Vec2)만 보유

## CTimeMgr (시간 관리자)

### 문제점
- 컴퓨터 성능에 따라 함수 호출 빈도가 달라짐
- 같은 시간에도 성능에 따라 처리량이 다름

### 해결 방법
1. **초기화**
   - 이전 카운트 값 (m_llPrevCount) 저장
   - 초당 카운트 횟수 (m_llFrequency) 저장

2. **업데이트**
   - 현재 카운트 값 (m_llCurCount) 측정
   - 한 프레임당 시간 (m_dDT) 계산
   - DT를 연산에 곱하여 프레임 독립적인 동작 구현

3. **추가 기능**
   - 윈도우 타이틀에 FPS 정보 표시

## CKeyMgr (키 입력 관리자)

### 문제점
- Windows의 `GetAsyncKeyState()`는 현재 상태만 제공
- 키 이벤트(방금 눌림, 방금 뗌) 감지 어려움

### 해결 방법
1. **키 상태 정의**
   - NONE: 이전에도 안눌림, 지금도 안눌림
   - TAP: 이전에 안눌림, 지금 눌림
   - HOLD: 이전에도 눌림, 지금도 눌림
   - AWAY: 이전에 눌림, 지금 안눌림

2. **구현 내용**
   - `m_vecKey` 벡터에 각 키 정보 저장
   - 키 상태와 이전 프레임 눌림 여부 추적
   - 윈도우 포커싱 상태 처리
   - 매 프레임마다 모든 키 상태 업데이트

3. **사용 방법**
   - CKeyMgr 싱글톤의 멤버 함수 호출로 키 상태 확인
   - `IsKeyTap()`, `IsKeyHold()`, `IsKeyAway()` 등 편의 함수 제공

</details>